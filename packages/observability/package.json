{
  "name": "@tripdrive/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Wrappers de logging estructurado y Sentry — usados por las 3 apps con el mismo DSN. ADR-051.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./logger": "./src/logger.ts",
    "./sentry-init": "./src/sentry-init.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sentry/nextjs": "^8.40.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
